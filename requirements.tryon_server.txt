# Try-on FastAPI server dependencies
# Install with: pip install -r requirements.tryon_server.txt
# (This pulls core PostVTON deps + server deps)
-r requirements.txt

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
